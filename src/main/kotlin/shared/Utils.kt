package shared

//import java.io.File
//import java.math.BigInteger
//import java.security.MessageDigest

/**
 * Reads lines from the given input txt file.
 */
//fun readInput(name: String) = File("src/main/kotlin/day13", "$name.txt").readLines()

//fun inputReader(day: Int, testData: Boolean = false, suffix: String = "") =
//    File("inputs", "Day${day.toString().padStart(2, '0')}${if (testData) "_test" else ""}$suffix.txt")
//        .inputStream()
//        .reader()

/**
 * Converts string to shared.md5 hash.
 */
//fun String.md5(): String = BigInteger(1, MessageDigest.getInstance("MD5").digest(toByteArray())).toString(16)
